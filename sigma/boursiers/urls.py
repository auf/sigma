# -*- encoding: utf-8 -*-

from django.conf.urls.defaults import patterns, url

# urlpatterns = patterns(
#     'sigma.boursiers.views',
#     url(r'^nouvelle_allocation/$', 'nouvelle_allocation',
#         name='nouvelle_allocation')
#     )
